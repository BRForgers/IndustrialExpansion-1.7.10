language: java

jdk:
- oraclejdk7
- oraclejdk8

branches:
  only:
    - master
    
before_install:
 - chmod +x gradlew

deploy:
  provider: releases
  api_key:
    secure: $GITHUB_API_KEY
  file: IndustrialExpansion-1.0d.jar
  skip_cleanup: true
  on:
    tags: false
